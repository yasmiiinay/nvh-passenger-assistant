# Runtime dependencies — Airport Multimodal Chatbot (AI7016)
# Resolution verified with `pip install --dry-run` on Linux x86_64 / Python 3.12 (see README environment notes).
# macOS arm64 installability must be confirmed by the local smoke test.
torch==2.9.1
torchvision==0.24.1
transformers==4.57.6
sentence-transformers==5.7.0
numpy==2.2.6
pandas==2.3.3
opencv-python-headless==4.12.0.88
gradio==5.50.0
jiwer==4.0.0
psutil==7.2.2
soundfile==0.13.1
pillow-heif==1.7.0   # HEIC photos straight from an iPhone; registered as a Pillow opener in src/vision.py
